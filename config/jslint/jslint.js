var options = {
    indent:     4,     // the indentation factor
    maxerr:     999,    // the maximum number of errors to allow
    maxlen:     80,
    white:      false,
    node:       true,
    browser:    true,
    predef:     ["_"]
};
